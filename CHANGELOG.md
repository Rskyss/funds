# 更新记录

本文件记录 [QDII 基金罗盘](https://github.com/Rskyss/funds) 的版本变更。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)（主版本.次版本.修订号）。

---

## [1.7.2] - 2026-09-01

1.7.1 部署到生产后自测发现的两处收尾，无用户可见功能变化。

### 修复

- **每日刷新脚本会误报失败**：1.7.1 把抓取并发降到 5 并加了限流退避，全量刷新从不到 5 分钟变成约 8 分钟；而 `scripts/scheduled-refresh.mjs` 用的 Node 内置 `fetch` 默认最多等 5 分钟响应头，脚本先超时报错退出（`UND_ERR_HEADERS_TIMEOUT`），服务端其实刷新成功。改用 `node:http` 请求、只设 30 分钟整体空闲超时；新增 3 个用例覆盖"延迟响应 / 冷却跳过 / 401 / 超时"。
- **刷新日志"指标回退"口径虚高**：最大回撤在刷新后一步才重算，之前被当成"抓取为空"计入（显示 723 只）。现按字段分别报（规模 / 经理 / 夏普 / 波动率 / 评级 / 申购状态 / 成立日），最大回撤不计。
- **1.7.1 引入的回归：当日净值快照全部被拒**：净值入库校验被 `Array.filter` 直接当回调，数组下标被当成"当前时间"，740 行全判为"未来日期"跳过（生产错误日志 `appendNavHistory 跳过 740 行无效净值`）。当天未丢数据（07:00 的旧代码已写入 8-31 净值），修法：校验函数忽略不像时间戳的第二参数 + 调用处改箭头函数 + 回归用例。

### 工程

- 推送规则：版本提交正文先列「解决问题：1.～n」，`.githooks/pre-push` 同步拦截（`git config core.hooksPath .githooks` 启用）。

### 说明

- 生产部署记录（1.7.1 → 1.7.2）见 `docs/安全加固_1.7.1/验收记录.md`「部署记录」。

---

## [1.7.1] - 2026-09-01

安全加固与工程补全版：不新增用户可见功能，修的是「别人能不能拿走你的数据」和「出了问题能不能知道」。

### 修复（安全）

- **线上数据库对公网敞开**：`user_profile`（含用户加密后的 API Key 与画像）、`chat_logs`（含 IP 与对话原文）、`chat_sessions`、`fund_doc_chunks` 四张表此前的访问策略对所有人全开，任何访客拿网页里公开的匿名 key 就能读、改、删全部内容。已删除这些策略并回收浏览器侧表权限，实测匿名 key 访问全部变为 401（`supabase/migrations/20260901031850_*.sql`）。
- **顺手清理**：删除已下线的匿名试用表与匿名可调用的特权函数；修复安全扫描报的函数 search_path 问题。
- **不设防的接口**：全量重抓（`/api/funds?refresh=1`）改为需授权（`DATA_REFRESH_TOKEN` / 后台登录 / 本机直连）且同一时刻只跑一份、10 分钟内不重复；用平台 Key 覆盖共享点评的接口改为仅限后台管理员；登录、注册、后台登录、Key 校验、点评重生成、埋点、经理页代理全部加限流；聊天限流改按用户计数（此前换个会话 id 就能绕过）；续写会话前校验归属（`server.mjs`）。
- **静态文件路径守卫**：旧判断会放行 `public_legacy` 这类同前缀目录，改为严格的目录内解析（`lib/http.mjs`）。
- **报错不再外泄内部信息**：未预期异常只返回通用文案 + 请求号，细节留在服务端日志；流式聊天同理。

### 修复（数据）

- **抓取零超时**：东方财富 13 处、百炼、Tavily、向量接口全部加超时（`lib/fetchTimeout.mjs`），上游挂起不再把整条刷新链路卡死。
- **排行接口格式漂移守卫**：字段数不足或代码非 6 位数字的行直接跳过；整批无法解析时中止刷新并报错，而不是把错位的数字写进库。
- **失败不再固化成空值**：费率/持仓/经理/评级/风险指标/净值历史抓取失败时抛错，脚本不再把空结果连同「已抓取」时间戳一起落库、之后永久跳过。
- **净值入库校验**：净值 ≤ 0、日期异常（未来/无法解析）的行不入库；净值历史写入按 200 行分片。
- **刷新不再把好数据刷成空**：每日全量刷新时某只基金的规模/夏普/波动率/经理/评级/申购状态页面偶发抓取失败，旧逻辑会用空值覆盖库里上次的好数据，前端再显示成「0.0 亿 / 0.00」。现在抓失败的字段沿用上次值（`lib/mergeMetrics.mjs`），抓取本身失败后自动重试一次，并在日志里打印当天缺失数量。实例：513310 中韩半导体ETF 东财页面数据齐全，9/1 刷新却被刷成空。
- **详情页缺数据显示「暂无」**：规模 / 最大回撤 / 夏普 / 波动率没有数据时显示「暂无」（灰色），不再显示成 0。
- **`npm run data:metrics` 升级**：默认只补缺（规模 / 夏普 / 波动率 / 经理任一为空的基金），一次同时补齐这四项；`--all` 全部重抓；并发与间隔可用 `METRICS_CONCURRENCY` / `METRICS_DELAY_MS` 调。
- **东财 HTTP 514 限流退避**：实测东财对突发请求很快回 514（并发 6 时 743 只里 222 次），这正是每天刷新后约两成基金规模/夏普为空的来源。刷新阶段并发由 10 降到 5（`METRICS_REFRESH_CONCURRENCY`），遇 514/429/503 重试前退避 3s。

### 新增（工程）

- `GET /api/health`：版本、运行时长、数据更新时间、数据库连通性，供 PM2/Nginx 探活。
- 进程级兜底：`SIGTERM`/`SIGINT` 优雅退出、`unhandledRejection` 记录、`uncaughtException` 记录后退出交给 PM2 重启；请求头/请求超时；`X-Content-Type-Options` 等安全响应头。
- `supabase/migrations/`：24 条线上迁移原样入仓 + `events` 表补录 + 本次收口迁移，数据库可从零重建（见 `supabase/README.md`）。
- `npm test`：单元测试从 5 个增至 20 个（路径守卫、限流、超时、解析守卫、净值校验）；`package.json` 声明 `engines.node >= 20.6`。
- 前端统一请求层 `frontend/src/api.js`（超时、取消、错误文案、401 自动登出）。

### 改进（前端）

- 列表筛选/排序改为只在条件变化时重算，搜索框输入不再每敲一个字全量重排 700 条。
- AI 回答流式中断时保留已经出来的内容，只在末尾标注中断原因；组件卸载时中止请求。
- 隐私模式下登录不再崩（本地存储写失败被吞掉）；本地登录态过期自动清理。
- 基金卡片可用键盘（Tab + Enter/空格）打开；后台各页加载失败有明确提示，不再永久「加载中…」。
- 修复详情抽屉关闭动画期间点「用我的模型重新生成」报错；删除原型期遗留的假数据与未使用组件（`data.js` 由 445 行减至 15 行）。
- 详情页专业指标的 (i) 提示框不再被卡片边框裁掉（之前因指标卡片 `overflow: hidden` 只露出半截）。

### 文档

- 修正版本号、注册方式（开放注册）、部署方式（rsync 而非 git pull）、`rules/*.md` 需重启、RLS 现状、`.env.example` 的死配置与缺失项；重写过期的 `AGENTS.md`；`docs/开发进度跟踪.md` 补记 1.4→1.7 版本与本次任务。

### 升级步骤（1.7.0 → 1.7.1）

1. 数据库：执行 `supabase/migrations/20260901031850_lockdown_sensitive_tables_drop_anon_trial.sql`（线上已于 2026-09-01 执行）。
2. 服务器 `.env` 新增 `DATA_REFRESH_TOKEN=<openssl rand -hex 16>`（可选但推荐；不加则刷新只接受本机直连）。
3. `npm run build` → rsync `server.mjs`、`lib/`、`public/`、`package.json`、`scripts/` → `pm2 restart funds` → `curl 127.0.0.1:3002/api/health` 看到 `"ok":true`。

---

## [1.7.0] - 2026-07-09

### 新增

- **热门筛选「标普500」标签**：按基金名称含「标普500」精确匹配（29 只），与「纳指100」同机制（`frontend/src/data.js`）。
- **热门筛选支持多选**：主题类标签（美股科技/纳指100/标普500/港股科技/黄金主题）之间取**并集**（点亮多个 = 同时看多个板块）；条件类标签（进攻型/晨星5★/可申购）与已选结果取**交集**（在结果里继续过滤）。再次点击取消，全部取消恢复全量（`frontend/src/App.jsx`、`components.jsx`）。
- **同类排名**：每只基金新增 `peerRank`（同主题第几名，1=最好）/ `peerCount`（同主题总数）字段；聊天推荐卡片的「观察分」改为展示「同类排名 N/M」，带分母、可解释（`lib/eastmoney.mjs`、`lib/agent/tools.mjs`）。
- **列表合并同基金多份额**：同一产品的 A/C 类、人民币/美元份额折叠为一张主卡（默认推人民币 A），列表由 735 张减至 572 张；主卡展示「另有 XX 份额(代码)」备注。复用聊天侧已有的份额识别逻辑（`lib/agent/shareClass.mjs`），仅作用于 `/api/funds` 列表响应，详情同类对比与聊天 Agent 仍用全量数据（`server.mjs`）。
- **AI 点评标注生成时间**：详情抽屉 AI 点评标签旁显示「生成于 YYYY-MM-DD」，长评优先取长评生成时间（`frontend/src/components.jsx`）。
- **匿名行为埋点与后台行为分析**（上次迭代遗留，本版一并入库）：前端 `track.js` 匿名上报页面浏览/打开基金/搜索/筛选事件至 `/api/events`，后台管理页新增行为分析视图（`frontend/src/Admin.jsx`、`server.mjs`、`lib/store.mjs`）。
- **评分单元测试**：新建 `tests/score.test.mjs`（`node --test tests/*.test.mjs`），覆盖年化换算、去重复计权、分组百分位与同类排名。

### 变更

- **评分公式去重复计权**：收益段由「近3月+6月+1年+今年以来+3年累计」五段改为「**近6月 + 近1年 + 3年年化**」三段不重叠区间，同一波行情不再被重复计分；删除「1 年收益额外加分」的二次计权；删除「美国宽基/底仓候选 +4 分」的写死品类偏好。近3月大跌惩罚、次新/高风险扣分、费率加减分保留（`lib/eastmoney.mjs`）。
- **百分位改同主题内计算**：「高关注/可观察/谨慎看待」由全体 QDII 混排改为**同主题内**排名——美股、黄金、债券各比各的，债券/商品类头名不再被科技类行情挤成低分；主题不足 6 只时标注「**同类样本少**」，不硬给结论。
- 详情结构化分析的「同区域排名」改用绝对分（`rawScore`）计算，避免跨主题聚合相对分。

### 修复

- **「黄金主题」死标签**：原按 `theme: "贵金属"` 过滤，但库内该主题为 0 只（实际归类为「商品资源」），点击永远无结果；改为按名称含「黄金」匹配（9 只）。

### 说明

- **无新增表/字段、无新增环境变量、无需重刷数据**：评分为服务读库时实时重算，重启即生效。
- 上线步骤：服务器 `git pull` → `pm2 restart funds`（`public/` 构建产物已随仓库更新）。
- 已知问题：`npm run agent:test` 12 个用例因 v1.6 BYOK 后聊天接口强制要求用户 Key 而全部失败（测试脚本未适配，与本版改动无关），待后续单独修复。

---

## [1.6.0] - 2026-06-25

### 新增

- **用户自带百炼 Key（BYOK）+ 每用户模型设置**：每个登录用户在头像菜单「模型设置」里配置三项——百炼 API Key、投问模型、短/长评模型。弹窗渐进式：先填 Key 点「验证 Key」，通过后才展开两个模型输入框；保存时会真实调用两个模型校验可用性，填错当场提示（`frontend/src/AiSettingsModal.jsx`、`server.mjs`）。
- **聊天投问改为自带 Key**：未配置 Key 的用户聊天禁用并显示「去设置」引导；已配置则用各自的 Key + 投问模型问答（非流式 403 / 流式 SSE `error` 事件，统一 `code: NO_AI_KEY`）。理解问题与生成回答全链路按用户单模型执行，不再走平台多模型降级（`lib/agent/planner.mjs`、`lib/agent/synth.mjs`）。
- **详情「用我的模型重新生成点评」**：已配置 Key 的用户可在基金详情用自己的 Key + 短/长评模型当场重生成短评/长评，仅本人可见、**不落库**，刷新即回到全站共享版（`POST /api/fund/:code/ai-summary/preview`）。
- **凭证安全**：用户 Key 经 **AES-256-GCM** 加密后存库（`lib/crypto.mjs`），后端永不向前端返回明文或密文，仅下发掩码（如 `sk-****abcd`）+ 模型名 + 配置状态。

### 变更

- 全站共享的短/长评（已预生成的 734 份）、向量检索、批量生成等**平台侧 AI 仍用后台 Key**，不受用户配置影响；普通访客无需配置即可浏览共享点评。
- `lib/ai.mjs` 的 `chatCompletion` / `chatCompletionStream` / `generateFundSummary` / `generateFundDetailSummary` 新增可选 `apiKey` 入参（缺省回退平台 env，行为不变）；新增 `validateAiCredentials` 校验函数。

### 数据库

- `user_profile` 新增三列：`ai_api_key_cipher`（加密密文）、`ai_chat_model`（投问模型）、`ai_review_model`（短/长评模型）。

### 配置

- 新增环境变量 `AI_KEY_SECRET`：64 位 hex（`openssl rand -hex 32`），用于加密用户 Key。**上线后不可更改**，否则已存的用户 Key 将无法解密。

### 说明

- 上线步骤：Supabase 执行 `user_profile` 增列 SQL → 服务器 `.env` 补 `AI_KEY_SECRET` → `npm run build` → 同步 `public/` 与后端 → `pm2 restart funds`。
- 安全提醒：`user_profile` 现存有加密 Key，建议核对其 RLS 仅允许「读自己那行」，避免公开 Key 越权读取（详见部署文档）。

---

## [1.5.1] - 2026-06-12

### 修复

- **会话历史时间显示**：历史列表的时间此前直接展示 UTC（伦敦）时间，比北京时间慢 8 小时（如 09:42 显示成 01:42）。改为按浏览器所在时区换算后再显示，与后台 `/admin` 时间显示口径一致（`frontend/src/components.jsx`）。

### 改进

- **AI 热议推荐自动更新**：热议问题的「是否需要更新」检查此前仅在服务进程启动时执行一次，长期运行（数周不重启）期间从不复查，导致开场热议长期不变。现接入每日定时数据刷新（07:00 `/api/funds?refresh=1`）：每日刷新后后台自动检查一次，板块当日异动（|涨跌| > 2.5%）当天即可更新，平稳期最多 30 天兜底刷新；失败静默降级，不影响主流程（`server.mjs`）。

### 说明

- 纯前端展示与后端触发时机调整，**无新增表/字段**。
- 上线步骤：`npm run build` → 同步 `public/` 与 `server.mjs` → `pm2 restart funds`。

---

## [1.5.0] - 2026-05-20

### 新增

- **服务端登录接口** `POST /api/auth/signin`：登录改走后端，统一邮箱规范化与密码首尾空白处理；错误信息中文化（`lib/authSignIn.mjs`）。
- **运维脚本** `npm run auth:reset-password`：管理员通过 Supabase Admin 重置指定用户密码（`scripts/reset-user-password.mjs`）。
- **前端错误边界**：根组件增加 `ErrorBoundary`，运行时异常可见报错与重试，避免整页白屏。

### 修复

- **申购限购展示**：东财返回「限大额」等无具体金额时，详情/列表/聊天卡片由「限购 null元/日」改为 **「大额限购」**。
- **登录流程**：前端不再直连 Supabase `signInWithPassword`，改调 `/api/auth/signin`，与注册路径一致。
- **注册/登录提示**：常见 Supabase 英文错误转为中文可读文案。

### 改进

- **顶栏品牌区**：文字品牌「QDII 罗盘 · FUND COMPASS · PRO」，可点击回首页。
- **详情·前 10 大重仓**：过滤无效持仓行；加载中与无数据分态；商品/债基主题显示对应说明。
- **详情·资产配置表**：增加「其他/期货等/债券等」列与堆叠条，补足 100% 构成展示。
- **详情·费率区**：赎回费率骨架高度收紧。
- **登录弹窗**：错误提示改用红色语义色。

### 生产部署

- **域名**：https://funds.aisoup.ai（Nginx 反代 → PM2 `funds`，端口 `3002`）
- **目录**：`/www/wwwroot/funds`
- **发版**：`npm run build` → 同步 `public/` 与后端 → `pm2 restart funds`

### 依赖与上线步骤

1. `npm install` → `npm run build` → 部署 `public/`、`server.mjs`、`lib/authSignIn.mjs`
2. `.env` 仍需 `SUPABASE_*`；无新增表/字段
3. 可选：`npm run auth:reset-password -- <邮箱> <新密码>`

### 已知限制（继承 v1.4）

- 多基金对比栏、偏好设置入口、完整筛选工具栏等仍未接入新 UI
- 详情持仓/费率仍可能首次为空（异步补抓）
- `npm run dev` 在部分代理环境下可能空白，验收建议 `build + start`

---

## [1.4.0] - 2026-05-20

### 新增

- **AI 热议推荐**：AI 投顾开场问题改为「2 条 🔥 实时热议 + 4 条固定推荐」；热议带暖色样式；板块日涨跌 |超过 2.5%| 或距上次生成超过 30 天触发重写；失败时静默降级为纯固定库。
- **管理后台**（`/admin`）：密码登录；概览（用户 / 邀请码 / 对话数）；用户列表；邀请码批量生成与删除；对话记录分页查看。
- **详情页 AI 长点评**：基金抽屉新增 250–350 字、3–4 段扩展点评（与列表卡片一句话区分）；新增 `rules/detail_blurb.md`；`npm run ai:detail` 批量生成（支持 `--limit` / `--force` / `--code`）。
- **聊天思考过程**：流式回复时展示可折叠的「思考过程」（模型开启 thinking 时）。
- **首页板块风向**：顶部由 KPI 四宫格改为「今日 QDII 板块风向」（主题只数 + 今日平均涨跌），点击可筛选列表。
- **只看自选**：列表支持「只看自选」；未登录点击引导登录。
- **排序升/降序**：收益、夏普、评级、规模等排序支持再次点击切换升序/降序。

### 改进

- **列表卡片迷你走势**：使用 `spark_json` 在卡片上展示净值迷你曲线（v1.3 已落库，本版接上 UI）。
- **AI 卡片短点评**：规则收紧为单句 30–42 字、只讲一个重点；输出自动去除 `(X字)`、外层引号等。
- **AI 投顾回答**：合成约束收紧（约 250 字内、基金只写 6 位代码、禁止重复卡片数字与引用清单）；推荐问题库约 16 条改写（去代码、口语化）。
- **打开基金交互**：再次点击同一只基金可关闭详情抽屉。
- **视觉**：主强调色调整为 `#3480F4`。
- **路由**：`/admin` 等前端路径支持 SPA 回退（直接访问不 404）。
- **文档**：`CLAUDE.md` 补充架构与运维说明；新增 `docs/ai-热议推荐/`（建表 SQL、上线步骤、交付说明）。

### 依赖与上线步骤

1. **Supabase 新表** `chat_hot_suggestions`（见 `docs/ai-热议推荐/migration.sql`）。
2. **`fund_ai_summary` 增加字段**（详情长点评，若尚未添加）：

```sql
alter table fund_ai_summary
  add column if not exists detail_summary text,
  add column if not exists detail_model text,
  add column if not exists detail_generated_at timestamptz;
```

3. **环境变量**：`.env` 配置 `ADMIN_PASSWORD`（管理后台）；热议生成仍需 `DASHSCOPE_API_KEY`，可选 `TAVILY_API_KEY`。
4. **发版命令**：`npm install` → `npm run build` → `npm start`；可选 `npm run ai:detail` 回填详情点评。

### 已知限制（相对 v1.3，仍未迁移）

- **多基金对比栏**（勾选最多 6 只、底部对比表）尚未接入新 UI。
- **偏好设置弹窗**（5 题画像）仍无入口；后端 `/api/profile` 与 `rules/persona.md` 仍可用。
- **筛选工具栏**：区域 / 主题 / 用途下拉、申购状态分段、列表分页/无限滚动仍为占位或未完整接入。
- **聊天多份额脚注**（`altShares`）尚未迁移。
- **详情持仓/费率**：首次请求可能为空（异步补抓），偶发需重开抽屉或清缓存。
- **开发模式**：系统 HTTP 代理可能导致 `npm run dev` 空白，验收建议 `npm run build && npm start`。

---

## [1.3.0] - 2026-05-19

### 重大变更

- **前端迁移至 Vite + React 18**：源码在 `frontend/`，`npm run build` 产出到 `public/`；v1.2 原生 JS 前端归档至 `public_legacy/`。
- **全新 UI**：Hero KPI 区、基金卡片、详情抽屉、AI 侧栏等整体重设计（Inter + JetBrains Mono 字体）。

### 新增

- **构建与开发命令**：`npm run dev`（Vite + 后端代理）、`npm run build`、`npm run preview`。
- **列表净值迷你曲线数据**：`funds.spark_json` 字段；刷新时自动生成；`npm run data:spark` 单独回填。
- **列表内存快照**：详情接口复用全表快照，减少重复查询。
- **服务启动自检**：数据早于最近定时更新时刻时，后台自动补刷。
- **前端本地缓存**：基金列表 7 天、详情 24 小时（`fundsCache` / `detailCache`）。
- **详情抽屉预览态**：打开时先用列表字段渲染，接口返回后再更新。
- **Agent 事件搜索优化**：按重仓股 + 主题拼搜索词；Tavily 改 `news` 主题、30 天内、结果去重。
- **合规卡**：新增「定投收益 vs 区间涨幅」禁止混算规则。

### 改进

- 详情页持仓、费率改为服务端异步补抓，首屏更快返回。
- 静态资源：`/assets/*` 长期缓存，HTML 入口 `no-cache`。
- Supabase SDK 从前端 CDN 改为 npm 包引入。

### 已知限制（相对 v1.2，后续版本补齐）

- **多基金对比栏**（勾选最多 6 只、底部对比表）尚未迁移到新 UI。
- **偏好设置弹窗**（5 题画像 → AI 千人千面）尚无入口；后端 `/api/profile` 与 `rules/persona.md` 仍可用。
- **筛选与排序**：区域 / 主题 / 用途下拉、申购状态分段、排序切换、「只看自选」、列表分页/无限滚动尚未接好（工具栏部分为占位 UI）。
- **列表卡片迷你走势图**：`spark_json` 已落库，列表卡 UI 尚未展示曲线。
- **聊天多份额脚注**（`altShares` 卡片备注）尚未迁移。
- **详情持仓/费率**：首次请求可能为空（异步补抓 + 详情缓存），偶发需关闭重开或清缓存后才看到完整数据。
- **开发模式**：系统 HTTP 代理可能导致 `npm run dev` 页面空白，日常验收请用 `npm run build && npm start`。

### 依赖说明

- 需在 Supabase `funds` 表增加 `spark_json`（json/jsonb 数组）字段。
- 发版前执行 `npm install` 与 `npm run build`，`npm start` 服务的是构建后的 `public/`。

---

## [1.2.0] - 2026-05-16

### 新增

- **邀请码注册**：注册需填写邀请码；服务端校验、占用与回滚；`npm run invite:gen` 批量生成邀请码写入 `invite_codes` 表。
- **用户画像「投资年限」**：偏好设置新增第 5 题（投资基金年限），写入 `user_profiles.fund_years`。
- **千人千面 AI 表达**：新增 `rules/persona.md` 策略卡，合成层按用户画像调整话术深浅与建议倾向。
- **推荐问题可配置化**：话术迁至 `rules/suggestions.md`，新增 `GET /api/chat/suggestions`；支持按基金动态替换回撤、夏普、评级等占位符。
- **快捷提问入口**：基金卡片与详情抽屉增加「向 AI 提问」按钮，一键带入当前基金上下文。
- **登录门槛**：未登录时勾选对比基金会引导登录。

### 改进

- 聊天推荐问题改为服务端下发，产品可直接改 `rules/suggestions.md` 而无需改前端代码。
- AI 合成（流式与非流式）注入用户画像，回答更贴合个人偏好。
- 偏好设置文案与说明优化（4 题 → 5 题）。

### 依赖说明

- 需在 Supabase 中已存在 `invite_codes` 表（字段：`code`、`status`、`expires_at`、`used_at`、`used_by` 等）。
- 需在 `user_profiles` 表增加 `fund_years` 字段（若尚未迁移）。

---

## [1.1.1] - 2026-05-16

### 文档

- `README.md` 全文改为中文，统一章节标题与说明表述。

---

## [1.1.0] - 2026-05-16

### 新增

- 首次开源发布：QDII 基金列表、筛选、对比、详情与净值图表。
- 东方财富 / 天天基金数据抓取与 Supabase 持久化。
- 用户登录、收藏、偏好设置。
- AI 基金短点评（DashScope）与聊天式 Agent（筛选 / 对比 / 概念 / 事件 / 持仓检索）。
- 数据回填与定时刷新脚本（F10、指标、持仓、经理、费率、向量等）。
