# 用户自带百炼 Key（BYOK）+ 每用户模型设置 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 每个登录用户在「模型设置」里填自己的百炼 API Key + 短/长评模型 + 投问模型；聊天投问强制用用户 Key+投问模型，未配置则禁用引导；短/长评默认显示全站共享版，配置后可在详情用自己的 Key+短长评模型当场重新生成（不落库）。

**Architecture:** 用户 3 项配置加密存入 `user_profile` 新增列。`lib/ai.mjs` 各生成函数加可选 `apiKey` 入参（缺省回退平台 env）。聊天路径（planner+synth）加 `creds={apiKey,model}` 入参。新增「临时重新生成点评」端点用用户凭证、不落库。前端头像菜单加「模型设置」渐进式弹窗，聊天面板加未配置引导态，详情点评区加「用我的模型重新生成」按钮。

**Tech Stack:** Node 原生 HTTP（`server.mjs`）、`node:crypto`、Supabase Postgres、Vite + React 18、百炼 DashScope（OpenAI 兼容）。

## Global Constraints

- 字段命名 DB `snake_case` / JS `camelCase`，映射集中在 `lib/store.mjs`。
- 敏感信息走 `.env`，**不提交 git**；新增 `AI_KEY_SECRET`。
- 项目**无测试套件、无 linter**：后端验证用 `npm run agent:test` + `curl`；前端用 `npm run build && npm start` 浏览器实测。
- 改 `.env` 后重启服务；改 `frontend/` 后必须 `npm run build`。
- 画像表名 `user_profile`（单数）。
- 锁定百炼，不支持其它平台 / Base URL；探测模型固定 `qwen-plus`。
- `lib/ai.mjs` 已有：`ENDPOINT`、`modelChain()`、`withModelFallback({models})`、`chatCompletion`、`chatCompletionStream`、`generateFundSummary`、`generateFundDetailSummary`。
- creds 约定：`{ apiKey: string, model: string } | null`；为 `null` 时一律回退平台 Key + modelChain（保证非聊天调用与回归不变）。

---

### Task 1: 数据库 3 列 + 加密工具 + 画像读写

**Files:**
- Create: `lib/crypto.mjs`
- Modify: `lib/store.mjs`（新增 `saveUserAiConfig` / `clearUserAiConfig`）
- Modify: `.env`、`.env.example`
- DB: `user_profile` 增 3 列

**Interfaces:**
- Produces:
  - `encryptSecret(plain): string`（`ivHex:tagHex:dataHex`）/ `decryptSecret(blob): string` / `maskSecret(plain): string`
  - `saveUserAiConfig(userId, { cipher, chatModel, reviewModel }): Promise<void>`
  - `clearUserAiConfig(userId): Promise<void>`
  - `getUserProfile(userId)` 返回行含 `ai_api_key_cipher` / `ai_chat_model` / `ai_review_model`

- [ ] **Step 1: 数据库增列**

Supabase SQL editor（或 supabase MCP `apply_migration`）执行：

```sql
alter table user_profile
  add column if not exists ai_api_key_cipher text,
  add column if not exists ai_chat_model text,
  add column if not exists ai_review_model text;
```

`getUserProfile` 用 `select("*")`，无需改查询。

- [ ] **Step 2: 生成并写入加密密钥**

```bash
openssl rand -hex 32
```

写入 `.env`：`AI_KEY_SECRET=<64 位 hex>`。在 `.env.example` 末尾追加占位：

```
# 用户自带 API Key 的加密密钥（AES-256-GCM）。用 `openssl rand -hex 32` 生成 64 位 hex。不要提交真实值。
AI_KEY_SECRET=
```

- [ ] **Step 3: 实现 `lib/crypto.mjs`**

```js
import crypto from "node:crypto";

const ALGO = "aes-256-gcm";

function getKey() {
  const hex = process.env.AI_KEY_SECRET;
  if (!hex || hex.length !== 64) {
    throw new Error("AI_KEY_SECRET 未配置或长度不对（需 64 位 hex / 32 字节）");
  }
  return Buffer.from(hex, "hex");
}

export function encryptSecret(plain) {
  const iv = crypto.randomBytes(12);
  const cipher = crypto.createCipheriv(ALGO, getKey(), iv);
  const enc = Buffer.concat([cipher.update(String(plain), "utf8"), cipher.final()]);
  const tag = cipher.getAuthTag();
  return [iv.toString("hex"), tag.toString("hex"), enc.toString("hex")].join(":");
}

export function decryptSecret(blob) {
  const [ivHex, tagHex, dataHex] = String(blob || "").split(":");
  if (!ivHex || !tagHex || !dataHex) throw new Error("密文格式不正确");
  const decipher = crypto.createDecipheriv(ALGO, getKey(), Buffer.from(ivHex, "hex"));
  decipher.setAuthTag(Buffer.from(tagHex, "hex"));
  const dec = Buffer.concat([decipher.update(Buffer.from(dataHex, "hex")), decipher.final()]);
  return dec.toString("utf8");
}

export function maskSecret(plain) {
  const s = String(plain || "");
  if (s.length <= 8) return "****";
  return `${s.slice(0, 3)}****${s.slice(-4)}`;
}
```

- [ ] **Step 4: 验证加解密往返**

```bash
node --env-file=.env -e "import('./lib/crypto.mjs').then(m=>{const c=m.encryptSecret('sk-test-1234abcd');console.log('cipher:',c);console.log('plain:',m.decryptSecret(c));console.log('mask:',m.maskSecret('sk-test-1234abcd'));})"
```

Expected：打印密文、解密回 `sk-test-1234abcd`、掩码 `sk-****abcd`。

- [ ] **Step 5: `lib/store.mjs` 新增 AI 配置读写**

在 `saveUserProfile` 之后追加：

```js
export async function saveUserAiConfig(userId, { cipher, chatModel, reviewModel }) {
  if (!userId) throw new Error("userId 必填");
  const { error } = await supabaseAdmin
    .from("user_profile")
    .upsert(
      { user_id: userId, ai_api_key_cipher: cipher, ai_chat_model: chatModel, ai_review_model: reviewModel, updated_at: new Date().toISOString() },
      { onConflict: "user_id" }
    );
  if (error) throw new Error(error.message);
}

export async function clearUserAiConfig(userId) {
  if (!userId) throw new Error("userId 必填");
  const { error } = await supabaseAdmin
    .from("user_profile")
    .upsert(
      { user_id: userId, ai_api_key_cipher: null, ai_chat_model: null, ai_review_model: null, updated_at: new Date().toISOString() },
      { onConflict: "user_id" }
    );
  if (error) throw new Error(error.message);
}
```

- [ ] **Step 6: 提交**

```bash
git add lib/crypto.mjs lib/store.mjs .env.example
git commit -m "feat(byok): 加密工具与画像 AI 配置(3项)读写"
```

---

### Task 2: `lib/ai.mjs` 支持传入 apiKey + 凭证校验

**Files:**
- Modify: `lib/ai.mjs`

**Interfaces:**
- Produces:
  - `chatCompletion` / `chatCompletionStream` / `generateFundSummary` / `generateFundDetailSummary` 均加可选 `apiKey`，缺省回退 `process.env.DASHSCOPE_API_KEY`
  - `validateAiCredentials({ apiKey, model }): Promise<{ ok, error? }>`

- [ ] **Step 1: `chatCompletion` 加 apiKey**

签名加 `apiKey: apiKeyArg,`（`signal,` 前）；函数体 `const apiKey = process.env.DASHSCOPE_API_KEY;` 改为 `const apiKey = apiKeyArg || process.env.DASHSCOPE_API_KEY;`。

- [ ] **Step 2: `chatCompletionStream` 加 apiKey**

同 Step 1 改 `chatCompletionStream`。

- [ ] **Step 3: `generateFundSummary` 加 apiKey**

签名 `generateFundSummary(fund, { model, signal } = {})` 改为 `generateFundSummary(fund, { model, apiKey: apiKeyArg, signal } = {})`；函数体 `const apiKey = process.env.DASHSCOPE_API_KEY;` 改为 `const apiKey = apiKeyArg || process.env.DASHSCOPE_API_KEY;`。

- [ ] **Step 4: `generateFundDetailSummary` 加 apiKey**

同 Step 3 改 `generateFundDetailSummary(fund, { model, cardSummary, signal } = {})` → 加 `apiKey: apiKeyArg,`，函数体取 key 改为回退写法。

- [ ] **Step 5: 新增 `validateAiCredentials`**

文件末尾追加：

```js
export async function validateAiCredentials({ apiKey, model } = {}) {
  if (!apiKey) return { ok: false, error: "缺少 API Key" };
  if (!model) return { ok: false, error: "缺少模型名" };
  try {
    const res = await fetch(ENDPOINT, {
      method: "POST",
      headers: { Authorization: `Bearer ${apiKey}`, "Content-Type": "application/json" },
      body: JSON.stringify({ model, messages: [{ role: "user", content: "ping" }], max_tokens: 1 }),
    });
    if (res.ok) return { ok: true };
    const text = await res.text().catch(() => "");
    return { ok: false, error: `${res.status}: ${text.slice(0, 200)}` };
  } catch (err) {
    return { ok: false, error: err.message };
  }
}
```

- [ ] **Step 6: 验证**

```bash
node --env-file=.env -e "import('./lib/ai.mjs').then(async m=>{console.log(await m.validateAiCredentials({apiKey:process.env.DASHSCOPE_API_KEY,model:'qwen-plus'}));console.log(await m.validateAiCredentials({apiKey:'sk-bad',model:'qwen-plus'}));})"
```

Expected：第一行 `{ ok: true }`；第二行 `{ ok: false, error: '401: ...' }`。

- [ ] **Step 7: 提交**

```bash
git add lib/ai.mjs
git commit -m "feat(byok): ai.mjs 各生成函数支持传入 apiKey 与凭证校验"
```

---

### Task 3: planner / synth 接受 creds

**Files:**
- Modify: `lib/agent/planner.mjs`、`lib/agent/synth.mjs`

**Interfaces:**
- Consumes: `chatCompletion({apiKey})` / `chatCompletionStream({apiKey})` / `withModelFallback({models})`
- Produces: `plan({...,creds})`、`synthesize({...,creds})`、`synthesizeStream({...,creds})`，`creds.model` 存在时只用该模型、`creds.apiKey` 透传

- [ ] **Step 1: planner 接受 creds**

签名加 `creds = null`。`withModelFallback` 调用改为：

```js
    const result = await withModelFallback(
      (model) =>
        chatCompletion({
          model,
          messages,
          json: true,
          temperature: 0.1,
          topP: 0.7,
          maxTokens: 600,
          apiKey: creds?.apiKey,
        }),
      { label: "planner", models: creds?.model ? [creds.model] : undefined }
    );
```

- [ ] **Step 2: synthesize 接受 creds**

签名加 `creds = null`；`const models = modelChain();` 改为 `const models = creds?.model ? [creds.model] : modelChain();`；其内 `chatCompletion({ model, messages, ... })` 加 `apiKey: creds?.apiKey,`。

- [ ] **Step 3: synthesizeStream 接受 creds**

签名加 `creds = null`；`const models = modelChain();` 改为 `const models = creds?.model ? [creds.model] : modelChain();`；其内 `chatCompletionStream({ model, messages, ... })` 加 `apiKey: creds?.apiKey,`。

- [ ] **Step 4: 回归验证**

```bash
npm run agent:test
```

Expected：与改动前一致（仍走平台 Key + modelChain，无 creds 报错）。

- [ ] **Step 5: 提交**

```bash
git add lib/agent/planner.mjs lib/agent/synth.mjs
git commit -m "feat(byok): planner/synth 支持 creds 单模型与自带 Key"
```

---

### Task 4: server.mjs 接线（profile + 聊天 + 临时重新生成）

**Files:**
- Modify: `server.mjs`

**Interfaces:**
- Consumes: `getUserProfile`、`saveUserAiConfig`/`clearUserAiConfig`（T1）、`encryptSecret`/`decryptSecret`/`maskSecret`（T1）、`validateAiCredentials`/`generateFundSummary`/`generateFundDetailSummary`（T2）、`planAgent`/`synthesize`/`synthesizeStream`（T3）
- Produces:
  - `GET /api/profile`：`profile` 内含 `aiChatModel`/`aiReviewModel`/`aiKeyMask`/`aiConfigured`
  - `POST /api/profile/ai/validate` `{aiApiKey}` → `{ok,error?}`（探测模型 `qwen-plus`）
  - `POST /api/profile` 接受 `aiApiKey`(空=不改)/`aiChatModel`/`aiReviewModel`/`clearAiKey`
  - `POST /api/chat` 未配置 → 403 `NO_AI_KEY`（流式发 `event: error`）
  - `POST /api/fund/:code/ai-summary/preview` → 用用户凭证临时生成、不落库

- [ ] **Step 1: import**

`getUserProfile` 所在 store import 行补 `saveUserAiConfig, clearUserAiConfig,`；`generateWithRetry` 所在 ai import 行补 `validateAiCredentials, generateFundSummary, generateFundDetailSummary,`；新增：

```js
import { encryptSecret, decryptSecret, maskSecret } from "./lib/crypto.mjs";
```

- [ ] **Step 2: 凭证辅助函数**

import 区后加：

```js
// 读取登录用户的聊天凭证（apiKey + 投问模型）；未配置/解密失败返回 null。
async function loadChatCreds(userId) {
  if (!userId) return null;
  const p = await getUserProfile(userId).catch(() => null);
  if (!p?.ai_api_key_cipher || !p?.ai_chat_model) return null;
  try { return { apiKey: decryptSecret(p.ai_api_key_cipher), model: p.ai_chat_model }; }
  catch { return null; }
}

// 读取用户的点评凭证（apiKey + 短/长评模型）。
async function loadReviewCreds(userId) {
  if (!userId) return null;
  const p = await getUserProfile(userId).catch(() => null);
  if (!p?.ai_api_key_cipher || !p?.ai_review_model) return null;
  try { return { apiKey: decryptSecret(p.ai_api_key_cipher), model: p.ai_review_model }; }
  catch { return null; }
}
```

- [ ] **Step 3: `GET /api/profile` 返回掩码与模型**

把：

```js
      const profile = await getUserProfile(tokenUser.userId);
      json(res, 200, { profile });
```

改为：

```js
      const profile = await getUserProfile(tokenUser.userId);
      let aiKeyMask = null;
      if (profile?.ai_api_key_cipher) {
        try { aiKeyMask = maskSecret(decryptSecret(profile.ai_api_key_cipher)); } catch { aiKeyMask = null; }
      }
      json(res, 200, {
        profile: {
          ...profile,
          aiChatModel: profile?.ai_chat_model || null,
          aiReviewModel: profile?.ai_review_model || null,
          aiKeyMask,
          aiConfigured: !!(profile?.ai_api_key_cipher && profile?.ai_chat_model),
        },
      });
```

- [ ] **Step 4: 新增 `POST /api/profile/ai/validate`**

在 `POST /api/profile` 路由块之前加：

```js
    if (url.pathname === "/api/profile/ai/validate" && req.method === "POST") {
      const tokenUser = await verifyToken(req.headers["authorization"]).catch(() => null);
      if (!tokenUser?.userId) { json(res, 401, { error: "请先登录" }); return; }
      const body = await readBody(req);
      const key = typeof body.aiApiKey === "string" ? body.aiApiKey.trim() : "";
      if (!key) { json(res, 400, { error: "请填写 API Key" }); return; }
      const check = await validateAiCredentials({ apiKey: key, model: "qwen-plus" });
      json(res, check.ok ? 200 : 400, check.ok ? { ok: true } : { ok: false, error: check.error });
      return;
    }
```

- [ ] **Step 5: `POST /api/profile` 处理 AI 配置**

在 `POST /api/profile` 体内、`RISK`/`HORIZON` 校验之前插入：

```js
      if (body.clearAiKey === true) {
        await clearUserAiConfig(tokenUser.userId);
        json(res, 200, { ok: true, aiConfigured: false });
        return;
      }
      if (body.aiApiKey !== undefined || body.aiChatModel !== undefined || body.aiReviewModel !== undefined) {
        const chatModel = typeof body.aiChatModel === "string" ? body.aiChatModel.trim() : "";
        const reviewModel = typeof body.aiReviewModel === "string" ? body.aiReviewModel.trim() : "";
        if (!chatModel || !reviewModel) { json(res, 400, { error: "请填写投问模型与短/长评模型" }); return; }
        const existing = await getUserProfile(tokenUser.userId).catch(() => null);
        let cipher = existing?.ai_api_key_cipher || null;
        let plainKey = null;
        if (typeof body.aiApiKey === "string" && body.aiApiKey.trim()) {
          plainKey = body.aiApiKey.trim();
          cipher = encryptSecret(plainKey);
        } else if (cipher) {
          try { plainKey = decryptSecret(cipher); } catch { plainKey = null; }
        }
        if (!plainKey || !cipher) { json(res, 400, { error: "请先填写 API Key" }); return; }
        const check = await validateAiCredentials({ apiKey: plainKey, model: chatModel });
        if (!check.ok) { json(res, 400, { error: `投问模型校验失败：${check.error}` }); return; }
        await saveUserAiConfig(tokenUser.userId, { cipher, chatModel, reviewModel });
        json(res, 200, { ok: true, aiConfigured: true, aiChatModel: chatModel, aiReviewModel: reviewModel, aiKeyMask: maskSecret(plainKey) });
        return;
      }
```

- [ ] **Step 6: `/api/chat` 读凭证 + 缺失拦截 + 透传**

`const userProfile = userId ? await getUserProfile(...)` 之后、`if (wantStream)` 之前加：

```js
      const chatCreds = await loadChatCreds(userId);
      if (!chatCreds) {
        if (wantStream) {
          res.writeHead(200, { "content-type": "text/event-stream; charset=utf-8", "cache-control": "no-store", connection: "keep-alive", "x-accel-buffering": "no" });
          res.write(`event: error\ndata: ${JSON.stringify({ message: "请先在「模型设置」中填写你的百炼 API Key", code: "NO_AI_KEY" })}\n\n`);
          res.end();
        } else {
          json(res, 403, { error: "请先在「模型设置」中填写你的百炼 API Key", code: "NO_AI_KEY" });
        }
        return;
      }
```

并在 `/api/chat` 内 4 处调用加 `creds: chatCreds`：流式 `planAgent(...)`、流式 `synthesizeStream(...)`、非流式 `planAgent(...)`、非流式 `synthesize(...)`。（`runPlan` 不变。）

- [ ] **Step 7: 新增临时重新生成端点**

在现有 `const regenMatch = url.pathname.match(/^\/api\/fund\/(\d{6})\/ai-summary$/);` 路由块之后加：

```js
    const previewMatch = url.pathname.match(/^\/api\/fund\/(\d{6})\/ai-summary\/preview$/);
    if (previewMatch && req.method === "POST") {
      const code = previewMatch[1];
      const tokenUser = await verifyToken(req.headers["authorization"]).catch(() => null);
      if (!tokenUser?.userId) { json(res, 401, { error: "请先登录" }); return; }
      const creds = await loadReviewCreds(tokenUser.userId);
      if (!creds) { json(res, 403, { error: "请先在「模型设置」中填写 API Key 与短/长评模型", code: "NO_AI_KEY" }); return; }
      const funds = await getAllFunds();
      const fund = funds.find((f) => f.code === code);
      if (!fund) { json(res, 404, { error: "未找到该基金" }); return; }
      const body = await readBody(req).catch(() => ({}));
      try {
        const card = await generateFundSummary(fund, { model: creds.model, apiKey: creds.apiKey });
        let detail = null;
        if (body.long === true) {
          const d = await generateFundDetailSummary(fund, { model: creds.model, apiKey: creds.apiKey, cardSummary: card.summary });
          detail = d.detail;
        }
        json(res, 200, { summary: card.summary, detail, model: creds.model, ephemeral: true });
      } catch (err) {
        json(res, 400, { error: `生成失败：${err.message}` });
      }
      return;
    }
```

> 注：`generateFundDetailSummary` 返回字段名以 `lib/ai.mjs` 实际为准（参考 `scripts/generate-detail-summary.mjs` 里 `const { detail, model } = await generateDetailWithRetry(...)`，即返回 `detail`）。

- [ ] **Step 8: 验证接口**

`npm start`（另开终端），取登录 token。

未配置时聊天拦截：

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:5173/api/chat -H "authorization: Bearer <TOKEN>" -H "content-type: application/json" -d '{"message":"推荐纳指基金"}'
```
Expected：`403`。

验证 Key：

```bash
curl -s -X POST http://localhost:5173/api/profile/ai/validate -H "authorization: Bearer <TOKEN>" -H "content-type: application/json" -d '{"aiApiKey":"<真实KEY>"}'
```
Expected：`{"ok":true}`。

保存配置：

```bash
curl -s -X POST http://localhost:5173/api/profile -H "authorization: Bearer <TOKEN>" -H "content-type: application/json" -d '{"aiApiKey":"<真实KEY>","aiChatModel":"qwen-plus","aiReviewModel":"qwen-plus"}'
```
Expected：`{"ok":true,"aiConfigured":true,...}`。之后聊天成功；`POST /api/fund/<某code>/ai-summary/preview` 返回 `summary` 且 `ephemeral:true`；DB 共享点评未变。

- [ ] **Step 9: 提交**

```bash
git add server.mjs
git commit -m "feat(byok): profile 三项配置接口、聊天用用户凭证、点评临时重生成端点"
```

---

### Task 5: 前端「模型设置」渐进式弹窗

**Files:**
- Create: `frontend/src/AiSettingsModal.jsx`
- Modify: `frontend/src/App.jsx`、`frontend/src/components.jsx`

**Interfaces:**
- Consumes: `authedFetch`、`/api/profile`、`/api/profile/ai/validate`
- Produces: `<AiSettingsModal open onClose onSaved />`；`App` 状态 `aiConfigured`、`aiSettingsOpen`；`TopBar`/`AIDrawer` 新增 props

- [ ] **Step 1: 创建 `AiSettingsModal.jsx`**

```jsx
import React, { useState, useEffect } from "react";
import { authedFetch } from "./auth.js";

export default function AiSettingsModal({ open, onClose, onSaved }) {
  const [apiKey, setApiKey] = useState("");
  const [chatModel, setChatModel] = useState("");
  const [reviewModel, setReviewModel] = useState("");
  const [mask, setMask] = useState(null);
  const [configured, setConfigured] = useState(false);
  const [validated, setValidated] = useState(false);
  const [error, setError] = useState("");
  const [busy, setBusy] = useState(false);

  useEffect(() => {
    if (!open) return;
    setError(""); setBusy(false); setApiKey(""); setValidated(false);
    authedFetch("/api/profile").then((r) => r.json()).then((d) => {
      const p = d?.profile || {};
      setMask(p.aiKeyMask || null);
      setChatModel(p.aiChatModel || "");
      setReviewModel(p.aiReviewModel || "");
      setConfigured(!!p.aiConfigured);
      if (p.aiConfigured) setValidated(true); // 已配置过：直接展开两个模型框
    }).catch(() => {});
  }, [open]);

  useEffect(() => {
    if (!open) return;
    const onKey = (e) => { if (e.key === "Escape") onClose(); };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [open, onClose]);

  if (!open) return null;

  async function validate() {
    setError(""); setBusy(true);
    try {
      const r = await authedFetch("/api/profile/ai/validate", { method: "POST", body: JSON.stringify({ aiApiKey: apiKey.trim() }) });
      const d = await r.json();
      if (!r.ok || !d.ok) throw new Error(d.error || "验证失败");
      setValidated(true);
    } catch (err) {
      setError(err.message || "验证失败");
    } finally { setBusy(false); }
  }

  async function save(e) {
    e.preventDefault();
    setError(""); setBusy(true);
    try {
      const payload = { aiChatModel: chatModel.trim(), aiReviewModel: reviewModel.trim() };
      if (apiKey.trim()) payload.aiApiKey = apiKey.trim();
      const r = await authedFetch("/api/profile", { method: "POST", body: JSON.stringify(payload) });
      const d = await r.json();
      if (!r.ok) throw new Error(d.error || "保存失败");
      onSaved?.(true);
      onClose();
    } catch (err) {
      setError(err.message || "保存失败");
    } finally { setBusy(false); }
  }

  async function clear() {
    setError(""); setBusy(true);
    try {
      const r = await authedFetch("/api/profile", { method: "POST", body: JSON.stringify({ clearAiKey: true }) });
      const d = await r.json();
      if (!r.ok) throw new Error(d.error || "清除失败");
      onSaved?.(false);
      onClose();
    } catch (err) {
      setError(err.message || "清除失败");
    } finally { setBusy(false); }
  }

  return (
    <div className="auth-modal" role="dialog" aria-modal="true">
      <div className="auth-modal__backdrop" onClick={onClose} />
      <div className="auth-modal__card">
        <button className="auth-modal__close" onClick={onClose} aria-label="关闭">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M18 6 6 18M6 6l12 12"/></svg>
        </button>
        <h2 className="auth-modal__title">模型设置</h2>
        <p className="auth-modal__hint">填你自己的阿里云百炼 API Key 与模型名。Key 加密存储、不回显。AI 投问与“用我的模型重新生成点评”都用你自己的 Key。</p>
        <form className="auth-modal__form" onSubmit={save}>
          <label className="auth-modal__field">
            <span>百炼 API Key{configured && mask ? `（已保存：${mask}，留空=不修改）` : ""}</span>
            <input type="password" autoComplete="off" value={apiKey}
              onChange={(e) => { setApiKey(e.target.value); if (!configured) setValidated(false); }}
              placeholder={configured ? "留空则沿用已保存的 Key" : "sk-..."} />
          </label>
          {!validated && (
            <button type="button" className="btn btn--primary" onClick={validate} disabled={busy || !apiKey.trim()}>
              {busy ? "验证中…" : "验证 Key"}
            </button>
          )}
          {validated && (
            <>
              <label className="auth-modal__field">
                <span>短/长评模型</span>
                <input type="text" value={reviewModel} onChange={(e) => setReviewModel(e.target.value)} placeholder="qwen-plus" required />
              </label>
              <label className="auth-modal__field">
                <span>投问模型</span>
                <input type="text" value={chatModel} onChange={(e) => setChatModel(e.target.value)} placeholder="qwen-plus" required />
              </label>
              <button type="submit" className="btn btn--primary" disabled={busy}>{busy ? "保存中…" : "保存"}</button>
            </>
          )}
          {error && <p className="auth-modal__error">{error}</p>}
        </form>
        {configured && (
          <div className="auth-modal__switch">
            <button type="button" onClick={clear} disabled={busy}>清除我的配置</button>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: `TopBar` 菜单加「模型设置」**

`components.jsx` 的 `TopBar` 签名加 `onOpenAiSettings`；在 `avatar-menu__pop` 内「退出登录」之前插入：

```jsx
                  <button className="avatar-menu__item" onClick={() => { setMenuOpen(false); onOpenAiSettings?.(); }}>
                    模型设置
                  </button>
```

- [ ] **Step 3: `AIDrawer` 未配置引导态**

`AIDrawer` 签名加 `aiConfigured, onOpenAiSettings`。body 三元在 `!loggedIn` 之后、`view === "history"` 之前插入：

```jsx
        ) : !aiConfigured ? (
          <div className="ai-gate">
            <div className="ai-gate__icon">
              <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.7"><path d="M12 2a3 3 0 0 0-3 3v3a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3z"/><path d="M19 11v1a7 7 0 0 1-14 0v-1M12 19v3"/></svg>
            </div>
            <h3>填写你的百炼 API Key 后即可使用</h3>
            <p>AI 问答使用你自己的阿里云百炼 API Key 与模型。点下方按钮在「模型设置」中填写后即可提问。</p>
            <button className="btn btn--primary ai-gate__btn" onClick={onOpenAiSettings}>去设置</button>
          </div>
```

输入框区：`onSubmit`/`onKeyDown` 的 `if (!loggedIn)` 分支改为 `if (!loggedIn || !aiConfigured) { if (!loggedIn) onRequireLogin?.(); else onOpenAiSettings?.(); return; }`；`placeholder` 加未配置文案 `!aiConfigured ? "填写 API Key 后即可提问"`；`textarea` 与发送按钮 `disabled` 由 `busy || !loggedIn` 改 `busy || !loggedIn || !aiConfigured`。

- [ ] **Step 4: `App.jsx` 接线**

1）import 加 `import AiSettingsModal from "./AiSettingsModal.jsx";`
2）状态加：
```js
  const [aiSettingsOpen, setAiSettingsOpen] = useState(false);
  const [aiConfigured, setAiConfigured] = useState(false);
```
3）effect（随 session）：
```js
  useEffect(() => {
    if (!session) { setAiConfigured(false); return; }
    authedFetch("/api/profile").then((r) => r.json())
      .then((d) => setAiConfigured(!!d?.profile?.aiConfigured)).catch(() => setAiConfigured(false));
  }, [session]);
```
4）`TopBar` 加 `onOpenAiSettings={() => setAiSettingsOpen(true)}`。
5）`AIDrawer` 加 `aiConfigured={aiConfigured}` `onOpenAiSettings={() => setAiSettingsOpen(true)}`。
6）`FundDrawer` 加 `aiConfigured={aiConfigured}`（供 Task 6 用）。
7）`AuthModal` 附近渲染：
```jsx
      <AiSettingsModal open={aiSettingsOpen} onClose={() => setAiSettingsOpen(false)} onSaved={(ok) => setAiConfigured(ok)} />
```

- [ ] **Step 5: 构建并手测**

```bash
npm run build && npm start
```

浏览器 http://localhost:5173 登录后：① AI 投顾抽屉显示未配置引导、输入禁用；② 头像菜单「模型设置」→ 弹窗只显示 Key + 「验证 Key」；③ 填真实 Key 点验证 → 出现两个模型框 → 填 `qwen-plus` 保存成功；④ 再开抽屉可正常提问。

- [ ] **Step 6: 提交**

```bash
git add frontend/src/AiSettingsModal.jsx frontend/src/App.jsx frontend/src/components.jsx public
git commit -m "feat(byok): 模型设置渐进式弹窗与未配置引导态"
```

---

### Task 6: 详情「用我的模型重新生成点评」

**Files:**
- Modify: `frontend/src/components.jsx`（`FundDrawer` 点评区）

**Interfaces:**
- Consumes: `authedFetch`、`POST /api/fund/:code/ai-summary/preview`、`FundDrawer` 的 `aiConfigured` prop（Task 5 已传）
- Produces: 详情点评区一个按钮 + 本地覆盖显示

- [ ] **Step 1: FundDrawer 接收 aiConfigured + 本地覆盖状态**

`FundDrawer({ fund, onClose, isFav, onFav, chatOpen, onOpenFund, onOpenChat, onCloseChat })` 签名加 `aiConfigured`。在组件内（与 `aiExpanded` 同处）加：

```js
  const [aiOverride, setAiOverride] = useState(null); // { summary, detail }
  const [regenBusy, setRegenBusy] = useState(false);
  const [regenErr, setRegenErr] = useState("");
```

- [ ] **Step 2: 点评区接入覆盖与按钮**

把 AI 点评区那段 IIFE 顶部：

```js
            const aiText = d.aiDetail || d.aiSummary || "";
```

改为优先用覆盖：

```js
            const aiText = aiOverride
              ? (aiOverride.detail || aiOverride.summary || "")
              : (d.aiDetail || d.aiSummary || "");
```

在 `ai-summary__head` 内（`AI 点评` tag 之后、展开按钮附近）加按钮（仅已配置时显示）：

```jsx
                    {aiConfigured && (
                      <button type="button" className="ai-summary__toggle" disabled={regenBusy}
                        onClick={async () => {
                          setRegenErr(""); setRegenBusy(true);
                          try {
                            const r = await authedFetch(`/api/fund/${fund.code}/ai-summary/preview`, { method: "POST", body: JSON.stringify({ long: true }) });
                            const dd = await r.json();
                            if (!r.ok) throw new Error(dd.error || "生成失败");
                            setAiOverride({ summary: dd.summary, detail: dd.detail });
                          } catch (err) { setRegenErr(err.message || "生成失败"); }
                          finally { setRegenBusy(false); }
                        }}>
                        {regenBusy ? "生成中…" : "用我的模型重新生成"}
                      </button>
                    )}
```

在 `ai-summary__body` 之后加错误提示：

```jsx
                  {regenErr && <p className="auth-modal__error">{regenErr}</p>}
```

> 需在 `components.jsx` 顶部确认已 `import { authedFetch } from "./auth.js";`（若未引入则补上）。

- [ ] **Step 3: 构建并手测**

```bash
npm run build && npm start
```

已配置 Key 的账号打开任一基金详情：点评区出现「用我的模型重新生成」→ 点击 → 片刻后点评文字被替换为你模型生成的版本；关闭详情再打开 → 回到共享版（未落库）。未配置 Key 的账号不显示该按钮。

- [ ] **Step 4: 提交**

```bash
git add frontend/src/components.jsx public
git commit -m "feat(byok): 详情点评支持用用户模型临时重新生成"
```

---

## 完成后

- 可选更新 `README.md` / `CHANGELOG.md`：聊天与点评重生成改为用户自带百炼 Key。
- 部署服务器（PM2 `funds`，路径 `/www/wwwroot/funds`）`.env` 补 `AI_KEY_SECRET` 并重启；Supabase 跑 Task 1 的增列 SQL。
- 老用户 `aiConfigured=false`，会被引导填 Key，无数据迁移。

## 自查对照（spec 覆盖）

| Spec 要求 | 对应任务 |
|-----------|---------|
| 头像菜单「模型设置」入口 | T5 S2 |
| 渐进式：验证 Key 后才出两个模型框 | T4 S4、T5 S1 |
| 三项加密存储（key+投问模型+短长评模型） | T1、T4 S5 |
| 聊天用用户 Key+投问模型，单模型无降级 | T3、T4 S6 |
| 未配置聊天禁用+引导 | T4 S6、T5 S3 |
| 短/长评默认共享、配置后可临时重生成不落库 | T4 S7、T6 |
| 重生成用用户 Key+短长评模型 | T2 S3/S4、T4 S7 |
| 掩码不回显完整 Key | T1 S3、T4 S3 |
| 清除配置 | T4 S5、T5 S1 |
| 共享点评/向量/批量不受影响 | T3（creds=null 回退）、runPlan 不变 |
| 新增 `AI_KEY_SECRET` | T1 S2 |
