# TODO — QDII 基金罗盘 · 待办与缺少配置

## 🟢 现在就能用
打开浏览器访问 http://localhost:5173 即可使用全部功能。

## 🤖 AI 点评（已接入阿里百炼 qwen-plus）
- 726 只基金每只都有专属 AI 点评，存在 Supabase `fund_ai_summary` 表
- 卡片上原来重复的写死文案，已替换为 AI 生成的差异化文案（带紫色左边竖条）
- 详情页有"AI 点评"区块和"重新生成"按钮，可手动重跑
- 全量首次生成成本约 ¥0.2，再生成单只约 ¥0.0003
- 重新跑全量：`npm run ai:generate -- --force`（不加 --force 则跳过已有）

## 🟡 强烈建议做（5 分钟内能做完）

### 1. 在 Supabase Dashboard 创建一个测试账号（图形界面）
- 进入 https://supabase.com/dashboard/project/pmqmkbwcpzjgzjbflnqo
- 左侧 Authentication → Users → Add User → 填邮箱密码
- 或者直接在前端右上"登录"→"注册一个"也行（本地版会自动确认邮箱）

### 2. 把净值历史"养肥"
- 现在 `nav_history` 表里只有 220 条（只有当期净值的基金会写入）
- 多点几次右上"刷新数据"按钮，每次都会追加一天的数据
- 也可以打开任何一只基金的详情，会自动抓近 90 天历史净值并入库
- 1-2 周后基本所有基金都会有 30+ 天的走势

## 🔴 上线前必须做（如果你打算公开发布）

### 1. Supabase 邮箱验证重新打开
- 当前为了本地测试方便，注册接口走 `admin.createUser({email_confirm:true})` 自动确认
- 上线前：在 `server.mjs` 中删除 `/api/auth/signup` 路由，改用 Supabase 默认的 `signUp`（前端走 SDK），强制邮箱验证
- 或者保留这个接口但加上邀请码/CAPTCHA 校验

### 2. 把 secret_key 放到部署环境变量
- 千万不要把 `.env` 文件提交到 GitHub
- 检查命令：`git status` 确认 `.env` 不在列表里 ✅ 已确认

### 3. 域名 / HTTPS / 部署
- 本期需求 "先本地跑起来"，没有做部署
- 推荐方案：Cloudflare Pages + Workers（境外友好）或腾讯云 / 阿里云轻量服务器（国内访问快）

## ⚪ 可以加但本期没做的功能

| 想法 | 复杂度 | 价值 |
|---|---|---|
| 基金经理画像 / 持仓穿透 | 中 | 投研价值高 |
| 跌幅/涨幅推送通知 | 中 | 用户粘性 |
| 自选组合 / 模拟仓位回测 | 高 | 进阶投资工具 |
| 移动端样式优化 | 低 | 用户体验 |
| 数据每日定时刷新（cron） | 低 | 不用手点刷新 |
| 多语言（英文） | 中 | 海外用户 |
| 基金对比页独立成页（不只是表格） | 低 | 视觉对比 |

## ❓ 我有疑问需要你确认

1. **服务器现在还在跑**（PID 在 `/tmp/qdii-server.log` 启动时打印的进程下），需要停吗？
   - 停掉：`lsof -i :5173 -P -t | xargs kill`
   - 留着：下次直接刷新浏览器即可

2. **测试账号已删除**（`test-qdii@example.com`），数据库是干净的，可以放心给别人看。

3. **是否需要做数据定时刷新**？现在每次手点"刷新数据"才更新。如要自动，可以在 `server.mjs` 加 `setInterval` 每天凌晨自动跑一次。
