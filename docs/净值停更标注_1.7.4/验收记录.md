# 验收记录 · 净值停更标注与占位净值修复（v1.7.4）

## 净值停更标注 + 占位净值修复（2026-09-02，✅ 用户验收通过，已发版 v1.7.4）

交付：净值停更的基金保留在列表但标注、不打分、沉底；AI 投顾不推荐；没有日期的净值一律当缺失。

- **口径（用户拍板）**：保留并标注、不隐藏；门槛落后全站最新净值 14 天；任何排序沉底且"数据不足"一并沉底；AI 投顾筛选剔除、点名说明不推荐；占位净值只改规则不清洗；先看结果再部署
- **实现（产品语言）**
  - 首页卡片：净值停更的基金标签行多一个红色「净值停更 N 天」；不打分；任意排序都排在最后（"数据不足"卡片紧挨在它们前面）；搜索仍可找到
  - 详情页：净值走势上方一行红色提示「净值日期 2026-07-20 · 已停更 43 天，可能已清盘或暂停运作，以下收益与评分均为旧数据」
  - AI 投顾：筛选/排行结果不含停更基金；点名查询时候选文本注明"净值停更 N 天…不要推荐"，聊天卡片评分位显示「净值停更 43 天」；合规卡片新增话术
  - 数据：没有日期的净值在抓取合并、排行解析、写库、读库四处统一当缺失；健康检查"净值缺失" 8 → 10
- **自测**
  - 自动化测试：71 项全过（新增 22 项）：`npm test`
  - 接口（本地起服务连线上库，只读）：`/api/funds` 里 018336 / 018337 `navStaleDays=43`、`score=null`、`label=净值停更`；其余基金评分正常；`/api/health` `missing.nav=10`、`warn=false`
  - AI 投顾工具（真实读库，未调大模型）：港股筛选 195 只、含停更 0；点名 018336 → `navStaleDays 43 / score null / scoreLabel 净值停更`
  - 页面结构（无头 Chrome dump-dom）：579 张卡片，「净值停更 43 天」出现 2 次，列表末尾两张为 018336 / 018337，前面是"数据不足"卡片
  - 浏览器走查（Chrome Playwright Extension，本地 5190 新构建）：搜索"华夏恒生中国企业"命中 2 张卡、均带「净值停更 43 天」标签（悬停提示净值日期 2026-07-20）；点开详情，净值走势上方红色提示「净值日期 2026-07-20 · 已停更 43 天，可能已清盘或暂停运作，以下收益与评分均为旧数据」；清空搜索后分别按夏普比率、晨星评级、基金规模排序并滚到底，579 张卡的最后两张都是 018336 / 018337，前面是"数据不足"卡片；本站控制台零报错（记录到的 8 条错误全部来自用户 Chrome 里另一个插件和百度统计脚本）。截图：`截图/01-搜索命中-卡片标签.png`、`截图/02-详情页停更提示.png`、`截图/03-按评级排序-列表末尾.png`
  - 独立代理复查 T1–T4：无阻塞，1 条建议修（日期写法兼容）已修并加测试
  - 清理：服务器上 4 个只读核对脚本已删；本地服务用后已停；没有新建账号；本地起服务时热议推荐检查为"跳过"，未写库
- **已声明的边界**
  - 14 天门槛是经验值；单只基金临时停更超两周会被标注沉底，恢复后自动消失
  - 停更基金退出同类样本后，同主题其它基金名次可能前移 1～2 位
  - AI 投顾真实聊天链路没有测试账号，只验证到工具层与上下文文本
  - 库里 003244 / 003245 的占位值"1"不清洗，靠读取时当缺失覆盖
- **用户验收**：2026-09-02，用户看过 3 张截图与自测结果后拍板"部署生产 + 推 GitHub"；无修正意见。发版 v1.7.4（生产已部署并验证，GitHub 已推）
