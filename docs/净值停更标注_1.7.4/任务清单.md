# 任务清单 · 净值停更标注与占位净值修复（v1.7.4）

状态：✅ 用户已批准开工（2026-09-02） · 方案见 [方案.md](方案.md)。每个任务：先写会失败的测试 → 实现 → 验证 → 勾掉。

| # | 任务 | 怎么验收 | 依赖 |
|---|---|---|---|
| ✅ T1 | 停更判定纯函数 `markStaleNav` + 常量 14 天（`lib/dataQuality.mjs`） | `npm test`：边界/全站参照/缺日期/空入参四组用例通过 | — |
| ✅ T2 | 评分排除停更：`applyPercentileScores` 调 `markStaleNav`，停更基金 `score=null`、`label="净值停更"`、不占同类样本 | `npm test`：新增用例通过，原有 47 个不变 | T1 |
| ✅ T3 | 读库路径：`getAllFunds` 打标；`rowToFund` 无日期净值当缺失 | `npm test`（新 `tests/store.test.mjs`）；本地起服务 `curl /api/funds` 看 018336 `navStaleDays≈43`、`score=null`；`/api/health` 净值缺失 8→10 | T1 |
| ✅ T4 | 兜底通道不再写占位净值：抽 `mergeFallbackInfo` 纯函数 | `npm test`（新用例：有日期收、无日期不收） | — |
| ✅ T5 | AI 投顾：`projectFund`/`pickCards`/`fundLine` 带停更信息；筛选/排行工具剔除停更；`rules/` 加一句话术 | `npm test`；代码走查列出改动点；本地起服务用管理员 curl 不可行（无测试账号），如实登记 | T3 |
| ✅ T6 | 前端：卡片标签、任意排序沉底（含数据不足）、详情停更提示、聊天卡片评分位显示「净值停更」、缓存键 v3 | `npm run build` 成功；本地 `npm start` + 无头 Chrome 截图：列表标签、四种排序末尾、搜索命中、详情提示 | T3 |
| ✅ T7 | 收尾：版本号 1.7.4、CHANGELOG、台账登记、验收记录；把成果发给用户 | 用户过目 | T1–T6 |
| ✅ T8 | （用户已拍板）部署生产 + 推 GitHub v1.7.4 | `/api/health` 版本 1.7.4；线上列表看到标签 | 用户批准 |

依赖顺序：T1 → T2/T3 → T5/T6 → T7；T4 独立，可穿插。

## 执行记录（2026-09-02）

- T1–T6 均按"先写失败测试 → 实现 → 通过"完成，本地提交 d0580ec / 443b8ca / c22bd8d / fc7517b / 8d314f3 / 10efd36；复查修复 2cc5e50；收尾 04f047f
- T1–T4 由独立代理复查：无阻塞；"日期写法只认 ISO"已修，测试缺口已补；T5 修正了复查指出的"AI 投顾读库带旧分"
- T6 排序逻辑抽成 `frontend/src/sortFunds.js` 用 node 单测；页面用无头 Chrome 核对结构（579 卡、2 张停更标签、停更卡在最末）；浏览器截图走查已用 Playwright Extension 完成（3 张截图在 `截图/`）
