# 任务清单 · 安全加固 1.7.1

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T1 | 线上库收口：删四条全开策略、回收表权限、清 anon_trial、修 search_path | 匿名 key 访问四张表 401；`consume_anon_trial` 404；advisor 无 ERROR | ✅ |
| T2 | `lib/http.mjs` / `lib/fetchTimeout.mjs` / `lib/rateLimit.mjs` / `lib/navValidation.mjs` + 单测 | `npm test` 全绿 | ✅ |
| T3 | `eastmoney.mjs`：13 处 fetch 加超时、`parseFundRow` 守卫、失败抛错 | 单测 + `node --check` | ✅ |
| T4 | `store.mjs`：净值校验、分片；`ai.mjs` / `tools.mjs` / `embedding.mjs` 超时 | `node --check`；无裸 `await fetch(` | ✅ |
| T5 | `server.mjs`：健康检查、刷新门禁+单飞、点评覆盖仅管理员、限流、会话归属、路径守卫、错误不外泄、优雅退出、安全头 | 本地起服务 curl 逐项验证 | ✅ |
| T6 | `scripts/scheduled-refresh.mjs` 带 token；`test-agent-cases.mjs` 支持 `AGENT_TEST_TOKEN` | `node --check` | ✅ |
| T7 | `supabase/migrations/` 26 个文件 + README | 文件与线上迁移历史一一对应 | ✅ |
| T8 | 前端：`api.js`、`auth.js`、`App.jsx` memo、`components.jsx` SSE/取消/键盘/修复/删死码、`Admin.jsx` 错误态、`main.jsx`、`data.js` | `npm run build` 成功；浏览器实测 | ✅ |
| T9 | `package.json`（test/engines/1.7.1）、tag v1.5.0–v1.7.0、lock 同步 | `git tag`；`npm ls` 干净 | ✅ |
| T10 | 文档：CLAUDE.md / README / AGENTS / CHANGELOG / .env.example / ARCHITECTURE / 台账 / 本目录三份 + 验收记录 | 内容与代码一致 | ✅ |
| T11 | 自测：单测、构建、接口 curl、浏览器走查、SIGTERM | 见 验收记录.md | ✅ |
| T12 | 本地提交（不推送、不部署） | `git log` 一条 `release: v1.7.1` | ✅（本地提交，待用户决定推送/部署） |
| T13 | 用户反馈：指标显示 0（刷新把好数据刷成空）+ (i) 提示框被裁 | 指标回退 + 重试 + 「暂无」+ CSS；浏览器实测 | ✅（并入 v1.7.1 提交） |
| T14 | 存量空值补跑 `npm run data:metrics`（库为本地/线上共用） | 规模缺失 155 → 3，夏普 187 → 67（余为不满一年），经理 64 → 0 | ✅（用户要求本地跑通后执行，两遍） |
| T15 | 东财 514 限流退避 + 刷新并发 10 → 5 | 第二遍补跑 0 次 514 | ✅ |

依赖：T2 → T3/T4/T5；T5+T8 → T11 → T12。
