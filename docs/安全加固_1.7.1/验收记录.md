# 验收记录 · 安全加固 1.7.1

日期：2026-09-01 ｜ 执行：Claude（`/loop` 自动模式）｜ 状态：⏳ 待用户验收（本地已全部自测通过；未推送、未部署）

## 1. 线上数据库（生产 Supabase，已执行）

用前端公开下发的 publishable key 直连 REST，删策略**前 / 后**对比：

| 表 | 之前 | 之后 |
|---|---|---|
| user_profile | 200，4 行全可读 | **401** |
| chat_logs | 200，117 行（含 IP、原话） | **401** |
| chat_sessions | 200，128 行 | **401** |
| fund_doc_chunks | 206，4034 行 | **401** |
| events / invite_codes | 0 行（RLS 无策略） | **401**（表权限也收了） |
| funds（公开数据） | 200 | 200（743 行，不受影响） |
| favorites | 0 行（仅本人策略） | 0 行（不变） |
| RPC consume_anon_trial | 可调用 | **404**（已删） |

安全扫描（`get_advisors security`）：ERROR 0；剩余 WARN 两条与本次无关（`vector` 扩展在 public schema、Auth 泄漏密码保护未开），INFO 若干为"RLS 开启但无策略"——这正是预期状态。

## 2. 单元测试

`npm test` → **20 / 20 通过**（原 5 个评分用例 + 新增：路径守卫 5、限流 2、超时 2、排行解析守卫 3、净值校验 3）。

## 3. 构建

`npm run build` → 成功，`public/assets/index-C5o87KtJ.js`（452 kB）。`node --check` 通过：server.mjs 与全部改动的 lib/scripts 文件。

## 4. 接口自测（本地 `PORT=5190 node --env-file=.env server.mjs`，连生产 Supabase）

| 项 | 期望 | 结果 |
|---|---|---|
| `GET /api/health` | 200，含 version/db | ✅ `{"ok":true,"version":"1.7.1","db":"ok","dataUpdatedAt":"2026-08-31T23:01:39Z"}`；响应带 `x-content-type-options` / `x-frame-options` / `referrer-policy` |
| `GET /..%2fpublic_legacy/index.html` | 404 | ✅ 404（修复前可读到旧版前端） |
| `GET /api/funds?refresh=1`（带 X-Forwarded-For，模拟公网） | 401 | ✅ 401「刷新数据需要授权」 |
| 同上 + 错误 `x-refresh-token` | 401 | ✅ |
| `POST /api/fund/000001/ai-summary` 无管理员 | 403 | ✅ 403「仅限后台管理员」 |
| 登录错密码连续 6 次（同邮箱） | 第 6 次 429 | ✅ `400 400 400 400 400 429` |
| 后台登录错密码连续 6 次 | 第 6 次 429 | ✅ `401 401 401 401 401 429` |
| 匿名聊天 + 非法 sessionId | 403 NO_AI_KEY | ✅ |
| 请求体 1.1 MB | 413 | ✅ 413「请求体过大」 |
| 非法 JSON | 400 | ✅ |
| 埋点正常上报 | 200 | ✅ `saved:1` |
| `GET /api/funds` | 200 | ✅ 579 只，更新时间 2026/9/1 07:00 |
| 静态首页 / hash 资源 | 200 / immutable | ✅ |
| `GET /api/fund/000834` | 200 | ✅ navHistory 315、analysis、holdings 30 |
| `kill -TERM` | 优雅退出 | ✅ 日志 `[shutdown] 收到 SIGTERM… 在途请求已处理完毕，退出`，进程退出码 0，端口释放 |

## 5. 浏览器走查（Playwright，`http://127.0.0.1:5190`）

- 首页加载：579 张基金卡片，每张为 `role=button` + `aria-label="查看 … 详情"`；顶栏「AI 投顾 / 登录」按钮正常
- 搜索「纳指」：计数变为 **11 / 579**
- 键盘：聚焦第一张卡片后按 **Enter** → 详情抽屉打开（`role=dialog.drawer`，内容含「中韩半导体ETF华泰柏瑞 513310 … 315 个数据点」）；按 **Escape** → 关闭
- `/admin`：显示「管理后台」登录框，「进入」按钮在密码为空时禁用
- 控制台：0 个来自本站的错误（8 条错误全部来自浏览器扩展与百度统计脚本）

## 6. 测试副作用（已如实记录）

- 本地起服务连的是生产库：启动自检判断数据已是最新、**未触发全量抓取**；但「热议推荐」检测到半导体板块 -3% 触发了一次生成（用平台 Key，写入 `chat_hot_suggestions` 两条新问题）——这是既有的启动行为，非本次改动引入
- 埋点表多了 1 行 `anon_id=smoke-test-1.7.1` 的 page_view；Supabase Auth 记录了 6 次针对虚构邮箱的失败登录；**未创建任何测试账号**，无需清理
- 之前生成的 `smoke-drawer*.jpeg`、`.playwright-mcp/` 已删除，后者已加入 `.gitignore`

## 6.5 追加：用户看图反馈的两个问题（同日）

**病根**（有数据）：
- 东财源头对 513310 数据齐全：用项目自己的解析代码实测 `fetchFundProfile` → 规模 134.65 亿（截止 2026-06-30）、经理「柳军、李沐阳」；`fetchFundRiskMetrics` → 夏普 2.10、波动率 54.71%。对照基金 000834 同样正常。
- 库里该基金 `aum_billion / sharpe_1y / volatility_1y / manager_names` 全为 null，`updated_at` = 今天 07:00 的刷新。结论：当天刷新时这只基金的两个页面偶发抓取失败，`upsertFunds` 无条件覆盖，把上次的好数据刷成了空；前端 `normalizeFund` 再把 null 变成 0。
- 同类规模：库里 155/743 只规模为空、187 只夏普为空。

**修法与验证**：
| 项 | 结果 |
|---|---|
| `lib/mergeMetrics.mjs` 指标回退 + 4 个单测 | `npm test` 24/24 |
| 东财抓取失败自动重试一次（`withRetry`，应用于评级/概况/风险/状态/兜底补全） | `node --check` 通过 |
| 刷新日志打印「指标回退 N 只 / 缺失 规模 x 只、夏普 y 只」 | 代码 |
| 详情页缺数据显示「暂无」（灰色） | 浏览器实测 513310：规模「暂无」、最大回撤 -36.73%、夏普「暂无」、波动率「暂无」，class `na` |
| (i) 提示框不被裁剪、不遮数值 | `.pro-grid` computed `overflow: visible`；提示框改以整个格子为定位基准，弹在格子**下方外侧**（computed `top: 格高+6px`），截图确认完整显示且「-36.73%」不被遮挡。无头浏览器里 `document.hasFocus()` 为 false 导致 `:focus` 不生效，验证时用强制显示类代替，`:hover/:focus` 规则未改动 |
| `npm run data:metrics` 一次补齐规模/夏普/波动率/经理 | 用户要求本地跑通后执行（库为本地与线上共用）。第一遍并发 6：更新 207 只、失败 86 只——失败全是东财 **HTTP 514 限流**（从第 14 行就开始，共 222 次），由此确认每天刷新后约两成基金指标为空的来源就是并发过高触发限流。第二遍并发 2 / 间隔 500ms：更新 155 只、失败 0、514 为 0 |
| 补跑后库内缺失 | 规模 155 → **3** 只；夏普/波动率 187 → **67** 只（剩余多为成立不满一年、东财无近 1 年指标）；经理 64 → **0**；最大回撤 17（净值历史不足） |
| 用户本机 dev 实例（localhost:5174）实测 513310 | 规模 134.7 亿 / 最大回撤 -36.73% / 夏普 2.10 / 波动率 54.71%；前端经 Vite 热更新生效；dev 后端（10:36 启动）仍为旧代码，需用户重启 `npm run dev` 才启用"刷新不覆盖好数据"保护 |
| 顺手加固 | `withRetry` 遇 514/429/503 退避 3s；刷新阶段指标并发 10 → 5（`METRICS_REFRESH_CONCURRENCY`）；补数脚本默认并发 3 / 间隔 300ms |

## 8. 部署记录（2026-09-01，用户指示"部署到服务器并完成自测和检查"）

**部署方式**：rsync（服务器目录非 git 仓库）。部署前备份 `/root/funds-backup-20260901_132258.tgz`（server.mjs / package.json / lib / scripts / public / .env）。

| 步骤 | 结果 |
|---|---|
| 本地预检 | `npm test` 全绿；重新 `npm run build` 后 `public/` 与 v1.7.1 提交零差异 |
| 服务器 `.env` | 追加 `DATA_REFRESH_TOKEN`（不覆盖原文件，值只存在服务器） |
| 同步 | `server.mjs` / `package.json` / `lib/`（排除 stock analysis，服务器 lib 212K）/ `scripts/` / `public/` / `supabase/` / `tests/` / 文档 |
| 服务器单测 | Node v24.11.1，`node --test --test-reporter=tap` **24/24**（Node 24 默认 spec 报告器不输出 `# pass`，第一次命令链因此在重启前中断——服务未中断，只是多花一步） |
| 重启 | `pm2 restart funds --update-env` → `/api/health` 返回 `version 1.7.1, db ok`；错误日志无新增 |
| Nginx | 确认 `proxy/funds.aisoup.ai/node.conf` 有 `X-Forwarded-For`，限流按真实访客 IP 计，不会互相误伤 |
| 接口自测（服务器本机 127.0.0.1:3002，模拟公网 XFF） | 安全头 ✓；路径穿越 404；`refresh=1` 无/错 token 401；覆盖点评 403；后台登录 6 次→429；登录同邮箱 6 次→429；匿名聊天 403 NO_AI_KEY；1.1MB→413；非法 JSON→400；列表 200（579 只，513310 规模 134.65/夏普 2.1）；静态 200 immutable |
| 公网（经 Nginx） | `/api/health` JSON ✓；首页 200；路径穿越 400（Nginx 先拒）；`refresh=1` 401；静态资源 `cache-control` 被 Nginx 的 `max-age=43200` 覆盖（既有配置，非本次问题） |
| 浏览器（Playwright 打开 funds.aisoup.ai） | 579 张卡片；513310 详情：规模 134.7 亿 / 回撤 -36.73% / 夏普 2.10 / 波动率 54.71%；`.pro-grid overflow: visible`，提示框 top = 格高+6px；列表卡片显示"夏普 2.10 · 回撤 -36.73%" |
| **真实刷新（cron 同款命令，第 1 次）** | 服务端全流程跑通：兜底补全 510/521、评级 740、专业指标 256s、**指标缺失（回退后）：规模 3 只，夏普 65 只**（部署前每天约 155 / 187）；回撤 723 只、曲线 729 只；热议更新。刷新后库内：规模缺失 3、夏普 67、经理 0，513310 数据保住 |
| 发现的问题 → 1.7.2 | ① 刷新脚本 `fetch` 默认 5 分钟响应头超时，而新刷新约 8 分钟 → 脚本先报"失败"退出（服务端其实成功），明早 cron 会天天误报 → 改 `node:http`，+3 个用例；② 日志"指标回退 723"把稍后重算的最大回撤也算进去 → 改按字段报、最大回撤不计 |
| 1.7.2 部署 | 同步 `server.mjs` / `lib/` / `scripts/` / `tests/` / `package.json` / `CHANGELOG.md` → 服务器单测 **27/27** → 重启 → `/api/health` `version 1.7.2, db ok` |
| **真实刷新（cron 同款命令，第 2 次，验证修复后的脚本）** | 脚本正常收到结果：`刷新完成：578 只基金，用时 458s`（超过 fetch 的 5 分钟上限，证明修复有效）；服务端：专业指标 255s，指标缺失（回退后）规模 3 / 夏普 65，无"指标回退"行（说明这次没有任何有旧值的字段抓空），回撤 723、曲线 729；两次刷新之间库内空值数不变、513310 数据保住 |
| **自测查出的回归（严重）** | 生产错误日志：`[store] appendNavHistory 跳过 740 行无效净值`——1.7.1 新加的净值校验被 `Array.filter` 直接当回调，数组下标被当成"当前时间"，740 行全判为"未来日期"，**当日净值快照一行没写**。当天没丢数据（07:00 的旧代码已写入 8-31 净值，两次手动刷新写的是同一天），但明早 07:00 起会天天丢。修法：校验函数忽略不像时间戳的第二参数 + 调用处改箭头函数 + 回归用例（740 行经 `filter` 全部通过）；本地/服务器单测 **28/28**；部署后在服务器用库里 3 只真实基金跑 `appendNavHistory` → `{"count":3}` |
| 最终状态 | 生产 `/api/health`：`version 1.7.2, db ok`；PM2 online；错误日志除 npm warn 无新增 |

## 7. 未覆盖项

- 真实 BYOK 聊天链路（需 `AGENT_TEST_TOKEN`，本次没有可用的已配 Key 用户 token）
- 生产部署与部署后的 `/api/health` 探活（未获授权）
- Nginx 是否给后端设置 `X-Forwarded-For`（影响"本机直连"豁免的安全性；配 `DATA_REFRESH_TOKEN` 后豁免自动失效，建议部署时一并配置）
